apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: laravel-test-istio
  namespace: production
spec:
  selector:
    matchLabels:
      app: laravel-test
      version: release
  endpoints:
    - port: http-envoy-prom
      path: /stats/prometheus
      interval: 30s
