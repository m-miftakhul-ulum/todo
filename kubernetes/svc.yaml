# kind: Service
# apiVersion: v1
# metadata:
#   name: laravel-test-svc
# spec:
#   type: NodePort
#   selector:
#     app: laravel-test
#   ports:
#   - protocol: TCP
#     port: 80
#     targetPort: 80



kind: Service
apiVersion: v1
metadata:
  name: laravel-test-svc
  namespace: production      
spec:
  type: ClusterIP           
  selector:
    app: laravel-test       
  ports:
  - name: http             
    protocol: TCP
    port: 80
    targetPort: 80